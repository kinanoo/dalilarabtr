-- ============================================================================
-- شريط الأسعار والأخبار — مفتاح تشغيل/إيقاف من لوحة التحكم
-- ============================================================================
-- شغّل هذا مرّة واحدة في Supabase ← SQL Editor.
-- بعدها يعمل زرّ التبديل في:
--   لوحة التحكم ← الإعدادات ← «إعدادات الموقع» ← «شريط الأسعار والأخبار المتحرّك».
-- القيمة الافتراضية = ظاهر (true)، فلا يختفي الشريط قبل أن تُطفئه بنفسك.
-- آمن للتشغيل أكثر من مرّة (IF NOT EXISTS).
-- ============================================================================

ALTER TABLE site_settings
  ADD COLUMN IF NOT EXISTS ticker_enabled boolean NOT NULL DEFAULT true;
