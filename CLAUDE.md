# دليل العرب — ملاحظات للعمل على المستودع

موقع محتوى عربي عن الحياة في تركيا. Next.js (App Router) + Supabase، منشور على
Cloudflare Workers عبر OpenNext.

## نشر المحتوى: القاعدة الأهم

المحتوى لا يُكتب في الكود — يُدرَج في Supabase عبر ملف SQL مؤرَّخ في `sql/`.
اتبع نمط الملفات الموجودة: رأس يوثّق المصادر، `WHERE NOT EXISTS` للأخبار أو
`ON CONFLICT (id) DO UPDATE` للمقالات، بلوك `DO $check$` يرمي `EXCEPTION` عند
الفشل، واستعلام مراجعة أخيراً.

### التطبيق آلي — لا نسخ ولا لصق

الملف الجديد يُطبَّق تلقائياً عند دمجه في `main` عبر
`.github/workflows/apply-sql.yml`، ويُسجَّل في `public.sql_migrations` فلا
يتكرّر. لا تطلب من صاحب الموقع تشغيل شيء يدوياً بعد الآن.

ما يترتّب على ذلك عند كتابة أي ملف:

- **الملف يُطبَّق داخل معاملة واحدة.** إن احتاج تنفيذاً خارجها (`VACUUM`،
  `CREATE INDEX CONCURRENTLY`) فضع في **السطر الأول** بالضبط:
  `-- @no-transaction`
- **لا تعدّل ملفاً مطبَّقاً.** التعديل لا يُعاد تطبيقه — يُنتج تحذيراً بتغيّر
  البصمة فقط. أي تصحيح يحتاج **ملفاً جديداً بتاريخ جديد**.
- **الترتيب أبجدي = زمني**، لأن الأسماء مسبوقة بالتاريخ. ملف يعتمد على آخر
  يجب أن يلي تاريخه.
- **الفشل يُسقط المعاملة كاملةً** ولا يُسجَّل الملف، فيُعاد تطبيقه بعد الإصلاح
  في ملف جديد — لا بقايا نصف مطبَّقة.

### قائمة تحقّق إلزامية عند إضافة أي خبر

الخبر وحده ناقص. قبل اعتبار المهمة منتهية، تحقّق من كل بند:

1. **هل يحتاج الخبر مقالاً مرافقاً؟** إن كان يشرح خدمة أو إجراءً يفعله القارئ
   بنفسه — فنعم. الخبر يُعلن، والمقال يشرح الخطوات. أنشئ الاثنين معاً في ملف
   واحد، واجعل `updates.link` يشير إلى `/article/<slug>` وليس إلى صفحة قائمة.

2. **أين مكان المقال في الموقع؟** حقل `articles.category` هو ما يقرّر ظهوره في
   الصفحات المجمِّعة. تحقّق من الصفحة المستهدفة قبل اختيار التصنيف:
   - `/e-devlet-services` ← `category = 'خدمات e-Devlet'` **و** `status = 'approved'`.
     الصفحة تستخدم `articles.source` لزر «زيارة الموقع» (ضع فيه الرابط الرسمي
     على `turkiye.gov.tr`) و`slug` لزر «اقرأ الشرح» — فإن تركت `source` فارغاً
     يظهر الكرت ناقصاً.
   - باقي الصفحات المجمِّعة (`/health`, `/work`, `/education`, `/residence`,
     `/housing`) تقرأ بتصنيفاتها العربية — افتح ملف الصفحة وتأكّد من قيمة
     `category` التي تستعلم بها بدل التخمين.

3. **`sql/supabase_schema.sql` مخطّط قديم لا مصدر حقيقة.** فيه أعمدة لم تعد
   موجودة (مثل `articles.is_active`) وتنقصه أعمدة أُضيفت بعده (`slug`,
   `status`, `tags`). ملفٌ بُنيت قائمة أعمدته عليه فشل فعلاً بـ
   `column "is_active" does not exist`.

   **أعمدة `public.articles` الفعلية** (مقروءة من القاعدة يوم 2026-08-17 —
   لاحظ `active` لا `is_active`، و`last_update`/`published_at` من نوع `date`
   لا نصّ):

   ```
   id:text (له افتراضي) · slug:text · title:text · category:text · status:text
   intro:text · details:text · content:text · excerpt:text
   steps:text[] · documents:text[] · tips:text[] · tags:text[]
   fees:text · warning:text · source:text · active:boolean · views:integer
   last_update:date · published_at:date · created_at:timestamptz · user_id:uuid
   image:text · image_url:text · seo_title · seo_description · seo_keywords:text[] · seo_image
   ```

   لذلك: **لا تكتب قائمة أعمدة مفترضة.** أدرِج الأعمدة المؤكَّدة وحدها
   (`slug`, `title`, `category`, `details`, `status`)، واضبط كل حقل إضافي في
   `UPDATE` مشروط بوجود عموده. و**حوّل إلى نوع العمود الفعلي**: تمرير نصّ
   مُعلَّم (`USING`) إلى عمود `date` يرفع
   `column is of type date but expression is of type text` — الحرفيّة غير
   المُعلَمة تُقبل ضمنياً والمُعلَمة لا تُقبل. اقرأ النوع من
   `pg_catalog.format_type` وابنِ `$1::<النوع>`.

   وفي المراجعة استعمل `to_jsonb(a) - 'details'` بدل تسمية الأعمدة — تعرض
   الموجود أيّاً كان ولا تفشل بعمود مفقود. النمط كاملاً في
   `sql/2026-08-17_car-master-guide-article.sql`.

   **واختبر على مخطّط منقول من القاعدة لا من فرضياتك.** فشل هذا الملف مرّتين
   لأن جدول الاختبار كان مبنيّاً على المخطّط القديم نفسه الذي أخطأ الملف
   باعتماده — فأكّد الفرضية بدل أن يكسرها.

4. **الروابط الداخلية: `scripts/_article-corpus.json` لقطة قديمة لا مصدر
   حقيقة.** وجود slug فيه لا يعني وجوده في القاعدة — حدث فعلاً أن مقالاً
   مُدرَجاً فيه كان مفقوداً، فأسقط بلوك التحقّق العمليةَ كلَّها وضاع الخبر معها.

   لذلك: **لا تجعل تحديث مقال شرطاً لنجاح خبر.** الخبر هو المُخرَج الأساسي.
   - استهدف **قائمة مرشّحين** بـ`WHERE slug IN (...)` بدل slug واحد.
   - احسم `updates.link` باستعلام فرعي يختار slug **موجوداً فعلاً**، واتركه
     `NULL` إن لم يوجد — رابط مكسور أسوأ من غياب رابط.
   - في بلوك `DO $check$`: `RAISE EXCEPTION` للخبر وحده، و`RAISE NOTICE`
     لتحديث المقال.

5. **الإشعارات:** الخبر الذي يُدرَج بـ`created_at` افتراضي يُشعِر المشتركين.
   عند إدراج أكثر من خبر دفعة واحدة، اجعل الثانوية `NOW() - INTERVAL '3 hours'`
   كي لا تنزل عدة إشعارات معاً.

6. **الوسوم:** `tags` تُستخدم في صفحات `/tag/<slug>`. الوسم `دليل` يُدرج المقال
   ضمن الأدلة العملية ويتطلّب 3 خطوات على الأقل في `steps`.

### التحقّق من المصادر

اربط كل خبر بمصدره الأوّلي (بيان رسمي، جريدة رسمية، موقع وزارة) لا بالوسيط
الذي نقله. لا تنسب المحتوى لقناة تلغرام أو صفحة تواصل. وإن ناقض المصدر الرسمي
ما يتداوَل، صحّحه صراحةً في المتن — الجمهور غالبيته سوريون تحت الحماية المؤقتة،
وتعميم قاعدة لا تنطبق على الكملك ضرر مباشر لا مجرد نقص دقة.

## الأداء: من أين تأتي قراءات قاعدة البيانات

الحصة على Supabase محدودة، والقاعدة العامة: **لا تقرأ من القاعدة في كل زيارة.**

- **`cookies()` يُلغي التخزين المؤقت.** أي صفحة عامة تبني عميل Supabase من
  `cookies()` تصير ديناميكية بالكامل، ويصبح `export const revalidate` بلا
  مفعول بصمت. للقراءات العامة استخدم `supabase` من `@/lib/supabaseClient`.
- **المسارات الديناميكية تحتاج `generateStaticParams`.** بدونها لا تُولَّد
  مسبقاً ولو صرّحت بـ`revalidate`. مع `dynamicParams = true` يبقى المحتوى
  الجديد يعمل عند الطلب.
- **لا تجلب أعمدة ثقيلة لا تستعملها** — `articles.details` و`updates.content`
  نصوص HTML كبيرة؛ سمِّ الأعمدة صراحةً في قوائم العرض.
- **بعد إطالة نوافذ ISR، فرّغ الكاش عند التحرير** عبر
  `POST /api/admin/revalidate` بالمسارات المتأثّرة (مطبَّق في `NewsManager`
  ومحرّر المقالات و`SiteBrowser`)، أو `{"scope":"site"}` للموقع كلّه حين
  يتغيّر شيء في التخطيط الجذري (مفتاح التواصل، رقم الواتساب) — الفورية بالحدث
  لا بالانتظار.
- **`revalidatePath` لا يحذف شيئاً بذاته.** يكتب وسماً في `tagCache`،
  والقراءة التالية تسأل `tagCache` هل الصفحة قديمة. تُرك المحوّل على
  الافتراضي `"dummy"` فترةً، و`isStale` فيه يرجع `false` دائماً — فكانت كل
  نداءات التفريغ ترجع 200 ولا تفرّغ شيئاً. الآن مربوط بـDurable Object
  (`NEXT_TAG_CACHE_DO_SHARDED` في `wrangler.toml`). إن رأيت تفريغاً «ينجح»
  دون أثر، ابدأ من هنا.
- **مخزن الكاش الدائم موجود:** حاوية R2 باسم `dalilarab` مربوطة عبر
  `NEXT_INC_CACHE_R2_BUCKET`، فصفحة ISR تُبنى مرة وتُخدَم لكل العُقد بدل
  إعادة البناء عند كل cold start.

## قبل اعتبار أي تغيير كود منتهياً

```
npx tsc --noEmit && npx jest --silent && npx next build
```

وراجع تصنيف المسارات في مخرجات البناء: `○` ساكن و`●` SSG جيّدان، و`ƒ` على صفحة
عامة يعني قراءة قاعدة بيانات في كل زيارة — تحقّق أن ذلك مقصود.
