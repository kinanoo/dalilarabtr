/**
 * Shared breakpoint constants — synced with Tailwind's default breakpoints.
 * Use these instead of magic numbers in JS/TS code.
 */
export const BREAKPOINTS = {
    sm: 640,
    md: 768,
    lg: 1024,
    xl: 1280,
    '2xl': 1536,
} as const;
